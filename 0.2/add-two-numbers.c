#include <stdio.h>
int main(){
	int i,a,x;
	printf("Enter thr first number: ");
	scanf("%d",&i);
	printf("Enter the second number: ");
	scanf("%d",&a);
	x=i+a;
	printf("The sum of %d and %d is: %d",i,a,x);
	return 0;
}
